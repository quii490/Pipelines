# 差异分析与可视化

差异分析必须基于明确的 `case`、`control` 和 biological replicates。先检查 count 分布、样本完整性、PCA/correlation 与批次，再解释 log fold change 和 FDR。

无生物学重复时不能可靠估计组内变异；探索性 effect-size 图必须明确标注，不得作为标准显著性检验替代。Volcano、heatmap 和 pathway 是结果摘要，不应取代原始 count 与设计检查。
